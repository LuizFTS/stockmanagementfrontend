export interface Product {
  id: string;
  name: string;
  description: string;
  inventoryBalance: number;
  active: boolean;
  costPrice: number;
  salePrice: number;
  createdAt: string;
  updatedAt: string;
}
